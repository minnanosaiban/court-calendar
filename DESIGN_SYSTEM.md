# court-calendar デザインシステム

作成: 2026-08-24。`public/style.css` を棚卸しして、フォントサイズ・カード（箱）の組み方を整理した記録。
hotline の `DESIGN_SYSTEM.md` と同じ位置づけ（今後の手直しで一貫性を崩さないための正本）。

サイト全体の方針（hotline と同じ系譜）：
- 罫線より余白・背景色差で区切る（`.minih`のラベル＋余白、カードの背景色差など。枠線はほぼ使わない）
- 見出しは太字でなくサイズで立てる（後述の型スケール）
- 装飾は無彩色寄り。使う色は基本、朱色（`--stamp`）とその薄い変種だけ

---

## 1. 文字サイズ（型スケール）

2026-08-23〜24に整理。それまで `font-size` が `.58rem`〜`1.8rem` の間で約30種類、1px刻みでバラついていた
（コピペで少しずつ増えた跡）。役割ごとに6段階へ統一した。

| 役割 | 値 | 使う場所 |
|---|---|---|
| サイト名 | **1.8rem** | `.masthead h1`（「応援傍聴ナビ」） |
| 主見出し | **1.6rem** | `.d-title`（事件名）・`.btitle`（掲示板見出し）・`.home-title` |
| 節見出し | **1.2rem** | `.lede-title`（裁判について）・`.subhead`（タイムライン・訴訟資料一覧）・`.modal .mhead`（編集モーダル見出し）・`.ctitle`（事件をさがす一覧の事件名）・`.month`（カレンダーの年月） |
| 本文 | **.9rem** | `.d-body`・`.facts`（ファクトシート）・`.pts`（箇条書き）・`.tl-head`（タイムライン）・`.bubble`（掲示板の投稿）・`.board-name`（掲示板の中の事件名）・`.pop-title`（カレンダーpopoverの事件名）・フォームの input/textarea/select・空状態のメッセージ（`.empty-msg`/`.board-empty`/`.u-empty`）など |
| キャプション | **.8rem** | 日付・法廷名などのメタ情報（`.tl-date`・`.mdate`・`.pop-date`・`.ud`/`.um`）・小さい編集/削除リンク（`.d-edit`・`.round-edit`・`.bubble .del`）・`.status`・`.site-footer`・`.fnote` など |
| ラベル | **.7rem** | 字間を広げた小見出し系（`.minih`・`.sech`・`.msec`）・`.mside-h`・`.tag`・`.btn`（資料の.pdf/.md/要約ボタン）・`.field label`（編集モーダルの各欄名） |

比率は主見出し:節見出し:本文 = 1.6 : 1.2 : .9 ≒ 4:3:3（節見出しと本文の間、主見出しと節見出しの間、
どちらも同じ1.33倍になるよう選んだ）。

**新しく文字サイズを足すときは、この6段階のどれかに乗せる**。独自の値（例：.84rem）を新設しない。

### 例外（型スケールに含めない）

以下はアイコン（`bi-*`）のグリフサイズや、カレンダーのマス目などUIコントロールの寸法として
個別に意図した値。文章の読みやすさとは別の理由でサイズが決まっているため、型スケールの対象外：

- `.like .bi`・`.facts .plink`（当事者リンクのアイコン）・`.btn .bi`：アイコングリフのサイズ
- `.cell .n`（カレンダーの日付数字）・`.bars .more`（「+N」表示）：カレンダーのマス目の寸法
- `.pop-arrow`：popoverの矢印アイコン

---

## 2. 余白スケール

2026-08-24に整理。それまで margin / padding が 2〜64px の間で30種類以上バラついていた
（5px・7px・9px・11px・13px・18px・22px・26px…と1px刻みで散っていた）。次の刻みに揃えた。

**基本は8px刻み（8 / 16 / 24 / 32 / 48 / 64）。細かいところだけ半分の4px刻み（4 / 12 / 20）を使う。**

| 値 | だいたいの使いどころ |
|---|---|
| 4px | ラベルと中身、行内の小さな間 |
| 8px | 段落と段落、リストの行間、小さな部品どうし |
| 12px | 見出しの直下、吹き出しどうし、カード一覧の間 |
| 16px | まとまりの区切り、カードの内側（狭いところ） |
| 24px | 節と節、カードの内側（スマホ） |
| 32px | セクションの区切り、カードの内側（タブレット以上） |
| 48px | 大見出し（`.home-title`・`.sech`）の上 |
| 64px | ページ下部の運用メモ（`.status`）の上 |

**対象外**：1〜3px の光学調整だけ（`.fnote code`の1px、`.mdate`の`padding-top:1px`、`.bars`の3px など）。
ボタン・タグ・入力欄など部品の padding も、下の「部品の大きさ」のとおり4px刻みに乗せてある。

### 見出しの上下
主見出し（1.6rem）の**下は12px**で統一する。上は役割で決まる：

| 見出し | 上 | 下 |
|---|---|---|
| 傍聴に行ってきたよ！掲示板 | 32px（パネルの内側余白） | 12px |
| 事件名 | 32px（カードの内側余白） | 12px |
| 期日カレンダー | 48px（セクションの区切り） | 12px |

---

## 3. 部品の大きさ（ボタン・タグ・入力欄）

2026-08-24に整理。padding は4px刻みに揃え、**同じ文字サイズ・同じ役割の部品どうしは同じ高さ**にする。

| 組 | 部品 | padding | 高さ |
|---|---|---|---|
| 小さいピル（.7rem） | `.btn`（.pdf/.md/要約）・`.tag` | `4px 12px` | 26.4px |
| 大きいピル（.8rem） | `.pillbtn`（詳細を見る等）・`.bwrite a`（傍聴の報告を書く）・`.pform button` | `8px 20px` | 41.3px |
| いいね（.8rem／掲示板の中は.7rem） | `.like`・`.board-name .like` | `4px 6px` | ─ |
| 入力欄（.9rem） | `.field input/textarea/select`・`.pform input/select` | `8px 12px` | 38.4px（selectのみ40px） |
| モーダルの保存/取消（.9rem） | `.mfoot button` | `8px 24px` | 42.6px |

**いいね（`.like`）だけは枠を持たない**（2026-08-24。それまでは枠線のピル型だった）。押した状態は
ハートの塗り（`bi-heart-fill`）と朱色で示せるので、面で囲う必要がない。「罫線より余白で区切る」という
サイト全体の方針にも合う。`padding` は枠が無くても指で押せる大きさを残すために置いてあり、見た目には出ない。
事件本体カードのいいねも掲示板の中のいいねも同じ扱いで、違うのは文字サイズだけ（.8rem／.7rem）。

高さをそろえるのに必要だった2つの手当て：

- **アイコン入りの部品には `line-height:1` を指定する**（`.btn .bi` `.like .bi`）。これらは
  `display:inline-flex` なのでアイコンは flex アイテムになり、`vertical-align` は効かず
  `line-height` がそのまま高さになる。指定しないと文字より背が高くなり、`.btn` が `.tag` より
  2.4px高くなっていた
- **`<button>` には `line-height:inherit` を指定する**（`.pform button` `.mfoot button`）。
  button 要素は line-height を継承せず既定の `normal` になるため、指定しないと同じ padding でも
  `.pillbtn` より8px低くなる

---

## 4. 角丸スケール

2026-08-24に整理。それまで 2/3/4/6/8/9/10/12/14/20px と10種類あったのを6種類に揃えた。

| 値 | 使いどころ |
|---|---|
| **4px** | 極小（`.fnote code`、`.lede`の左側） |
| **8px** | サムネイルなど小さな面（`.imgrow .ithumb`、`.stamp`） |
| **12px** | 箱の中の箱（`.bubble` `.arg` `.pform` `.cell` `.u` `.pop-row`、入力欄） |
| **20px** | カード・モーダル・浮かぶ面（`.card` `.modal` `.daypop` `.gal`） |
| **999px** | ピル型（ボタン・タグ）と、端を丸めた棒（`.bar` `.leg i` `.ubar`） |
| **50%** | 円（`.tl-dot` `.cell .n` `.share` `.cicon`＝事件アイコン） |

細い棒（高さ3px・幅4px）は 2px/3px を指定していたが、実効値は高さの半分で頭打ちになるので
意味の分かる `999px`（＝端を丸める）に統一した。

---

## 5. カード（箱）の組み方

### 基本
`.card{background:var(--card);border-radius:20px}` が土台。個別のカードはこれに `padding` などを足す
（`.dcard`など）。罫線は使わず、背景色（白 `--card`／生成りに替えて朱の薄色）だけで箱を表す。

### 「写真・掲示板・事件本体」は1つの塊（2026-08-23〜24）
事件ページ・トップの「最近の期日」カードは、次の3つが縦に並ぶ：

1. `.gal-card`（写真。事件ページのみ、写真があるときだけ）
2. `.bpanel`（傍聴に行ってきたよ！掲示板。常に先頭〜写真の直後）
3. `.dcard`（事件本体：タイトル・タグ・シェア・ファクトシート・裁判について・タイムライン等）

この3つは**隙間を空けずに続けて出し、1つの塊として見せる**。角丸は塊の外側（写真がある場合は写真の上／
事件本体の下）だけにし、継ぎ目（写真の下・掲示板・事件本体の上）は角丸にしない：

```
.gal-card{border-radius:20px 20px 0 0}      /* 上だけ丸める */
.bpanel{border-radius:20px 20px 0 0}         /* 既定＝先頭に来るとき、上だけ丸める */
.gal-card + .bpanel{border-radius:0}         /* 写真の直後に来るときは四角のまま */
.bpanel + .dcard{border-radius:0 0 20px 20px}/* 下だけ丸める */
.gal-card + .dcard{border-radius:0 0 20px 20px}/* 掲示板が非表示のとき（下記） */
```

写真が無い事件（トップページ、または事件ページで写真未登録）は掲示板が先頭に来るので、掲示板の上だけ
丸まる。写真があるときは、写真の上だけが丸まり、掲示板は四角になる。

**3枚のうち1枚が欠ける並びも必ず用意する。** 2026-08-25に事件ごとの掲示板の表示・非表示
（`cases.board_enabled`）を足したことで、「写真 → 事件本体」（掲示板なし）という並びが増えた。
このとき`.dcard`は既定の`.card`（四隅とも丸い）に戻り、継ぎ目に角丸が出てしまう。隣接セレクタで
組み合わせを列挙している以上、**表示条件を増やしたら並びの組み合わせも見直す**こと。現在ありうる並びは
次の4つで、いずれも「先頭の上」と「末尾の下」だけが丸くなる：

| 並び | 角丸 |
|---|---|
| 写真 → 掲示板 → 事件本体 | 写真の上／事件本体の下 |
| 写真 → 事件本体（掲示板が非表示） | 写真の上／事件本体の下 |
| 掲示板 → 事件本体（写真が未登録） | 掲示板の上／事件本体の下 |
| 事件本体のみ | 四隅（`.card`の既定のまま） |

**3枚の内側の余白（padding）は同じ値にそろえる**（スマホ24px／560px以上32px）。ずれていると、
写真・掲示板見出し・事件名の左端が縦にそろわず、1つの塊に見えない。例外は`.gal-card`の下だけで、
写真をカード下端まで塗り切る負のマージン（`.gal-card .gal:last-child{margin-bottom:-24px}`）と
対にするため24pxで固定する。

### 掲示板（`.bpanel`）の背景色
生成り（`#f4f1e8`）から、カレンダーの「期日がある日」と同じ薄い朱色 `rgba(185,50,38,.055)` に変更
（2026-08-24）。サイト内の朱系トーンを1つに揃える意図。

### 掲示板の中の事件名（`.board-id`）
掲示板だけが独立した箱になったため、どの事件の掲示板か分かるよう、見出し「傍聴に行ってきたよ！掲示板」の
直後にアイコン・事件名・いいね・「報告を書く」を表示する。事件名（`.board-name`）は`.d-title`と同じ
主見出しサイズではなく、本文サイズ（.9rem、`.tl-type`と同じ）に落として一段小さく表示する。いいねは
事件本体側のボタンと状態を共有しており、どちらを押しても両方に反映される。

**行頭はアイコン**（56px、2026-08-24）。見出しの「傍」・下の事件本体カードのアイコン（`.d-head`）と
左端が縦にそろい、掲示板と事件本体が1つの塊に見える。いいねは事件名の**後ろ**に置く（それまでは行頭
だった）。いいねは枠を持たないので（サイト共通。「3. 部品の大きさ」参照）、事件名にそのまま寄り添う形になる。

アイコンの右は`.board-id-main`として、**「事件名」「報告を書く」を縦に2段**重ねる（2026-08-24。
アイコンが56pxに大きくなり、事件名1行だけだとアイコンの下半分が余白になったため）。スマホでは事件名
自体が2行に折り返すことも多いが、右側の列を1つのまとまりとして縦に積んでいるだけなので、ボタンが
事件名から離れて見えることはない。

### 報告フォームの先頭の事件名（`.pform-case`）
報告が溜まると上の `.board-id` はスクロールで画面外に出るため、フォームを開いたときは先頭に同じ
アイコン＋事件名をもう一度出す（2026-08-24）。「どの裁判への報告か」が投稿ボタンの直前で必ず目に入るように
するのが目的。区切りは罫線1本（`border-bottom`）だけにして、下のフォーム本体と混ざらないようにする。
報告が少ないうちは `.board-id` とすぐ近くで2回出て少しくどいが、条件分岐を増やさない方を優先している。
事件名は折り返して全文を出す（省略記号にすると、取り違え防止のための行なのに肝心の事件名が読めなくなる）。

### カレンダーのポップオーバー幅
既定280pxだったが、56pxの事件アイコンを横に置くと本文（日付・事件名・種別）が窮屈になるため340pxに拡張
（`index.html`の`pw=Math.min(340, window.innerWidth-24)`、2026-08-24）。アイコンを日付の上に積む案も
検討したが、その場合プレビューの高さがほぼ倍になり、期日が3〜4件ある日はスマホ画面に収まらなくなるため
見送った。狭い画面では従来どおり`window.innerWidth-24`で頭打ちになる。

### ヘッダーと掲示板の間の案内ボタン
`.jumplink`（期日カレンダーを見る／事件をさがす）は上24px・下32pxの余白をとり、「帯」として独立させる
（2026-08-24。それまで下が8pxしかなく、掲示板パネルに貼り付いて窮屈だった）。掲示板パネル側に
margin を足すのではなくボタン列側で調整するのは、`.bpanel`に margin を付けると事件ページで
写真カードとの継ぎ目に隙間ができ、「1つの塊」が崩れるため。

---

## 6. 色トークン

`:root` で定義（`public/style.css` 冒頭）。

| トークン | 値 | 用途 |
|---|---|---|
| `--bg` | `#f3f2ee` | ページ全体の背景 |
| `--card` | `#ffffff` | カードの背景 |
| `--ink` | `#221f1a` | 本文の文字色 |
| `--mut` | `#6b695f` | 補助テキスト（白地でコントラスト比 約5.3:1・WCAG AA） |
| `--faint` | `#a8a59a` | 飾り（箇条書きの点・区切り）専用。文字には使わない |
| `--stamp` | `#b93226` | 朱色。見出しの一部・掲示板の背景（薄めて使用）・カレンダーの印など |
| `--tint` | `#f8f7f4` | ごく薄いグレー地（タグの背景など） |
| `--edit` | `#3f6d91` | 編集操作（＋追加・編集リンク）専用。白地でコントラスト比 約5.5:1（WCAG AA） |

---

## 7. 今後の整理候補

- ~~カード周りの `padding` の棚卸し~~ → **対応済み**（2026-08-24、「2. 余白スケール」）
- ~~角丸半径のスケール化~~ → **対応済み**（2026-08-24、「4. 角丸スケール」）
- ~~ボタン・タグ・入力欄の padding と高さの統一~~ → **対応済み**（2026-08-24、「3. 部品の大きさ」）
- 影（`box-shadow`）はまだ棚卸ししていない。`0 1px 2px rgba(0,0,0,.06)`（ピル）・
  `0 2px 12px rgba(0,0,0,.07)`（カードのホバー）・`0 14px 40px rgba(0,0,0,.2)`（popover）・
  `0 20px 60px rgba(0,0,0,.22)`（モーダル）と、高さごとに4種類ある。段階として整理できる
- 文字色に使っている個別の色（`#47705a` 原告／`#4a5a74` 被告／`#8a5c46` 裁判官／`#2f7d4f` コピー完了）は
  トークン化されておらず、`.arg h4` `.mside-h` `.bwho` の3か所に同じ値が直書きされている

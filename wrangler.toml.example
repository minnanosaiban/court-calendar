# これは公開用のひな形です。
# このファイルをコピーして wrangler.toml を作り、実際の値を記入してください。
# （wrangler.toml は .gitignore 済み＝GitHub には公開されません）

name = "court-calendar"
compatibility_date = "2026-06-01"
pages_build_output_dir = "public"

# 共有データベース（D1）
[[d1_databases]]
binding = "DB"
database_name = "court-calendar"
database_id = "YOUR_D1_DATABASE_ID"   # `npx wrangler d1 list` で確認できるID

# 訴訟資料のファイル置き場（R2）。R2 はアカウントでの有効化（支払い方法の登録）が要る。
# 有効化しないうちは、PDF を public/docs/ に置いて deploy し、資料の「ファイルのURL」欄に
# /docs/ファイル名.pdf を入れる運用でも動く。
# R2 を有効化したら、下の3行のコメントを外して `npm run r2:create` → deploy すれば画面からアップロードできる。
# [[r2_buckets]]
# binding = "FILES"
# bucket_name = "court-calendar-files"

[vars]
# 第1段階: 書き込みは OWNER_EMAIL のみ。
# 第2段階に進むときは ALLOW_ALL_WRITES = "true" に変えて再デプロイすれば、
# Access を通った人（＝閲覧を許可した人）全員が入力できるようになる。
ALLOW_ALL_WRITES = "false"
OWNER_EMAIL = "you@example.com"        # 書き込みを許可する本人のメール

# 行ってきたよ掲示板のスパム対策（Turnstile）。
# Site Key は公開されてよい値なのでここに書く。Secret は次のコマンドで入れる：
#   "<secret>" | npx wrangler pages secret put TURNSTILE_SECRET --project-name <プロジェクト名>
# どちらも未設定のうちは、一般の人の投稿は通らない（運営だけが書ける）。
TURNSTILE_SITEKEY = ""
# トークンが本物のサイト上で取られたことの照合先（カンマ区切り・本番のホスト名）。
TURNSTILE_HOSTNAMES = "your-project.pages.dev"

# ローカル開発の擬似ログインは .dev.vars（git管理外）に LOCAL_DEV="true" と DEV_EMAIL を書く。
# 本番にはこれらを置かないので、本番は必ず Access の実ログインが要る。

# ↓ Cloudflare Access のアプリを作った後に設定する（それまでは空）。
#   CF_ACCESS_TEAM_DOMAIN = "yourteam.cloudflareaccess.com"
#   CF_ACCESS_AUD = "（Access アプリの Application Audience タグ）"
CF_ACCESS_TEAM_DOMAIN = ""
CF_ACCESS_AUD = ""

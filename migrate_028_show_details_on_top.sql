-- 「争点・当事者」〜「関連裁判」を、事件ページだけでなくトップページのピックアップカードにも
-- 表示するかどうかを事件ごとに選べるようにする一度きりのマイグレーション（2026-08-28）。
-- これまでは常に非表示（事件ページでだけ見える）に固定していた。
-- ADD COLUMN は既存の行を消さない。既存の事件はすべて0（非表示のまま）で変わらない。

ALTER TABLE cases ADD COLUMN show_details_on_top INTEGER NOT NULL DEFAULT 0;

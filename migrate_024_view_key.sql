-- 事件ごとの閲覧制限（view_key）に向けた下準備の列を追加する（2026-08-26）
-- 値があればこの事件は非公開（キーが一致する人にだけ見せる）、NULLなら今まで通り誰でも公開。
-- この時点ではAPI側のフィルタは未実装で、列を追加するだけ（既存の事件はすべてNULL＝公開のまま変わらない）。
-- ADD COLUMN は既存の行を消さない。

ALTER TABLE cases ADD COLUMN view_key TEXT;

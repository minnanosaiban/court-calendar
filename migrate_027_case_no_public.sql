-- 事件番号を「公開してもよい場合だけ」表示できるようにする一度きりのマイグレーション（2026-08-28）
-- これまで事件番号（case_no）は運営専用の内部メモで、常に非公開だった。
-- 公開してよいかどうかをチェックボックスで選べるようにするための列を追加する。
-- ADD COLUMN は既存の行を消さない。既存の事件はすべて0（非公開のまま）で変わらない。

ALTER TABLE cases ADD COLUMN case_no_public INTEGER NOT NULL DEFAULT 0;
